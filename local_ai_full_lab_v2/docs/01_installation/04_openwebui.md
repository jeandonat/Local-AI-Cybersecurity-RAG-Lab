# OpenWebUI setup

This project assumes OpenWebUI is installed and configured to point at **native Ollama**.

## Access
- OpenWebUI: `http://localhost:3000`
- Ollama API: `http://localhost:11434`

## Configure OpenWebUI → Ollama
In OpenWebUI settings:

- **Ollama API Base**: `http://localhost:11434`

## Verify connection
If OpenWebUI shows your installed models, connectivity is correct.
