# JD Master persona

This persona is built as an Ollama custom model via `Modelfile`.

Build:
```bash
ollama create jd-master -f personas/jd-master/Modelfile
```

Use:
- Select `jd-master` in OpenWebUI.
